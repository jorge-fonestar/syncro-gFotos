# Google Photos API Configuration
# Obtén credentials.json siguiendo las instrucciones en README.md
CREDENTIALS_FILE=credentials.json

# Ruta donde se guardarán las fotos y videos descargados
# Puede ser una ruta local o de red (NAS)
# Ejemplos:
#   - Ruta local: C:\Users\TuUsuario\Pictures\GooglePhotosBackup
#   - Ruta de red (NAS): \\192.168.1.100\fotos\backup
#   - Ruta de red con letra: Z:\fotos\backup
DOWNLOAD_PATH=C:\Users\Yorch\Pictures\GooglePhotosBackup

# Archivo donde se guarda el estado de sincronización (items ya descargados)
STATE_FILE=sync_state.json

# Archivo de token de autenticación (se genera automáticamente)
TOKEN_FILE=token.pickle

# Tamaño de página para requests a la API (max 100)
PAGE_SIZE=100

# Reintentos en caso de error de descarga
MAX_RETRIES=3

# Tiempo de espera entre reintentos (segundos)
RETRY_DELAY=5
